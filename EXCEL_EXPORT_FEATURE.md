# قابلیت خروجی اکسل از لیست معلمان

## خلاصه
این قابلیت به مدیر مدرسه اجازه می‌دهد تا لیست کامل معلمان را به همراه **رمز عبور اولیه** آنها در قالب فایل اکسل دریافت کند.

## ویژگی‌ها

### 📊 اطلاعات موجود در فایل اکسل:

1. **نام معلم** - نام کامل معلم
2. **شماره همراه** - شماره تلفن همراه
3. **ایمیل** - آدرس ایمیل (در صورت وجود)
4. **رمز عبور** - رمز عبور اولیه (برابر با شماره همراه)
5. **دروس تدریس** - لیست دروسی که معلم تدریس می‌کند
6. **تعداد کلاس‌ها** - تعداد کلاس‌هایی که معلم در آنها تدریس دارد
7. **وضعیت** - فعال یا غیرفعال
8. **تاریخ ثبت‌نام** - تاریخ ایجاد حساب کاربری

### 🔐 امنیت رمز عبور

⚠️ **نکته امنیتی مهم**: رمز عبور اولیه هر معلم برابر با **شماره همراه** آن معلم است.

- معلمان می‌توانند بعد از اولین ورود، رمز عبور خود را تغییر دهند
- فایل اکسل حاوی رمز عبور اولیه است (نه رمز عبور تغییر یافته)
- مدیر باید این فایل را در مکانی امن نگهداری کند

### 📁 فرمت فایل

- **نوع فایل**: Excel (.xlsx)
- **نام فایل**: `لیست_معلمان_[نام مدرسه]_[تاریخ].xlsx`
- **مثال**: `لیست_معلمان_دبیرستان_شهید_رشیدی_1404-09-05.xlsx`
- **ساختار**: یک Sheet با نام "معلمان"
- **ستون‌ها**: 8 ستون با عناوین فارسی

## نحوه استفاده

### از طریق رابط کاربری:

1. به صفحه **مدیریت معلمان** بروید:
   ```
   http://localhost:3000/dashboard/principal/teachers
   ```

2. روی دکمه **"خروجی اکسل"** (سبز رنگ) کلیک کنید

3. فایل اکسل به صورت خودکار دانلود می‌شود

### از طریق API:

```bash
GET /api/principal/teachers/export
```

**Headers:**
```
Cookie: user_session=[session_cookie]
```

**Response:**
- Content-Type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Content-Disposition: `attachment; filename="لیست_معلمان_....xlsx"`

## فایل‌های مرتبط

1. **API Route**: `/src/app/api/principal/teachers/export/route.ts`
   - مسئول تولید فایل اکسل
   - دریافت اطلاعات معلمان از دیتابیس
   - ایجاد و فرمت‌بندی فایل اکسل

2. **Frontend Component**: `/src/app/dashboard/principal/teachers/page.tsx`
   - دکمه "خروجی اکسل"
   - مدیریت حالت loading
   - دانلود فایل

3. **Library**: `xlsx`
   - کتابخانه برای ایجاد فایل‌های اکسل
   - نصب شده با `npm install xlsx`

## مثال خروجی

| نام معلم | شماره همراه | ایمیل | رمز عبور | دروس تدریس | تعداد کلاس‌ها | وضعیت | تاریخ ثبت‌نام |
|---------|-------------|-------|----------|------------|--------------|-------|---------------|
| احمد محمدی | 09121234567 | ahmad@email.com | 09121234567 | ریاضی هفتم متوسطه دوره ۱, علوم هفتم دبستان | 3 | فعال | 1404/08/15 |
| زهرا احمدی | 09129876543 | - | 09129876543 | فارسی هشتم متوسطه دوره ۱ | 2 | فعال | 1404/08/20 |

## محدودیت‌ها

- فقط مدیر مدرسه (role: `principal`) می‌تواند به این قابلیت دسترسی داشته باشد
- فایل شامل همه معلمان مدرسه است (فعال و غیرفعال)
- رمز عبورها در فایل به صورت متن ساده (plain text) ذخیره می‌شوند

## نکات امنیتی

⚠️ **مهم**:

1. فایل اکسل را در مکانی امن نگهداری کنید
2. پس از تحویل اطلاعات به معلمان، فایل را حذف کنید
3. از ارسال این فایل از طریق ایمیل یا پیام‌رسان‌های عمومی خودداری کنید
4. در صورت امکان، فایل را با رمز عبور محافظت کنید
5. به معلمان توصیه کنید در اولین ورود، رمز عبور خود را تغییر دهند

## رفع مشکل

### دکمه "خروجی اکسل" غیرفعال است:
- بررسی کنید که حداقل یک معلم در سیستم ثبت شده باشد
- صفحه را Refresh کنید

### خطا در دانلود فایل:
- بررسی کنید که به عنوان مدیر مدرسه وارد شده‌اید
- Console مرورگر را بررسی کنید (F12)
- لاگ‌های سرور را بررسی کنید

### فایل دانلود نمی‌شود:
- بررسی کنید که مرورگر شما اجازه دانلود فایل را بلاک نکرده باشد
- تنظیمات popup blocker را بررسی کنید

## به‌روزرسانی‌های آینده

ویژگی‌های پیشنهادی:
- [ ] فیلتر کردن معلمان (فقط فعال‌ها یا غیرفعال‌ها)
- [ ] انتخاب ستون‌های مورد نظر برای export
- [ ] export به فرمت PDF
- [ ] ارسال خودکار اطلاعات ورود به ایمیل معلمان
- [ ] رمزگذاری خودکار روی فایل اکسل

---

**نسخه**: 1.0
**تاریخ**: 1404/09/05
**توسعه‌دهنده**: Claude AI
